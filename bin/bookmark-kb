#!/usr/bin/env bash
# Resolve the real location of this script (follows symlinks)
SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_DIR"
exec "$PROJECT_DIR/node_modules/.bin/tsx" "$PROJECT_DIR/src/index.ts" "$@"
